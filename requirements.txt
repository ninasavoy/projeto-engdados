# =============================================================================
# Dependências de runtime do pipeline (host venv + imagem Airflow + API).
#
# O Apache Airflow em si NÃO entra aqui (vem da imagem base apache/airflow:3.0.6).
# Para libs que o Airflow 3 também usa (sqlalchemy, requests, structlog, pyarrow,
# prometheus-client, psycopg2), usamos FAIXAS em vez de pins exatos: assim, ao
# instalar sobre a imagem do Airflow COM o arquivo de constraints (ver Dockerfile),
# o pip alinha essas versões às do Airflow sem rebaixá-las e quebrar o task-sdk.
# No host (sem constraints), o pip pega versões recentes compatíveis.
# =============================================================================

# Libs que o Airflow NÃO fornece — pin fixo (não entram em conflito):
pika==1.3.2                 # cliente RabbitMQ
psycopg[binary]>=3.2.4      # conexões diretas (psycopg v3)
python-dotenv>=1.0.0

# Libs compartilhadas com o Airflow — faixas alinhadas pelas constraints:
pandas>=2.1.4
pyarrow>=16.1.0             # backup Parquet (Airflow/google provider exige >=16.1)
requests>=2.32.0
structlog>=24.1.0
prometheus-client>=0.19.0
sqlalchemy>=1.4.49,<2.0     # mesma faixa exigida pelo Airflow 3
psycopg2-binary>=2.9.9      # driver do dialeto SQLAlchemy postgresql+psycopg2

# OpenTelemetry — tracing distribuído (rubrica A#1). SDK 1.27.0 é o que o
# Airflow 3.0.6 já traz; instrumentações 0.48b0 são o par compatível.
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp==1.27.0
opentelemetry-instrumentation-requests==0.48b0
opentelemetry-instrumentation-sqlalchemy==0.48b0
